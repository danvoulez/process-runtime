# Canonical specification

This directory holds the semantic vessel (see CANONICAL_KERNEL.md).

- PROCESS_RUNTIME_SPEC.md — current canonical contract (v0.5)
- PROCESS_RUNTIME_SPEC_v0.4.md — archived v0.4

v0.5 closes the law behind the implementation: every canonical behavior
shipped since v0.4 is now normative in the spec (authority key anchoring
and rotation, reconciliation vs lifecycle, duplicate-admission rules,
closed command set, lease worker binding, trust anchor verification).
